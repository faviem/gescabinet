<div style="display: block;" class="modal fade in" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="false">
<form  class="ui-draggable" style="position: relative; left: 424px; top: 229px;" action="{{ path('bz_user_profil',{'id': id})}}" method="post"  >
<div class="col-lg-4">
    <div class="panel panel-success">
        <div class="panel-heading" style="font-size: 13px;">
            MISE A JOUR DE PROFIL
        </div>
        <div class="panel-body" >
           <p>Membre :  <strong> {{ membre.nomPrenom }} </strong> </p>
           {{ form_widget(form) }}
        </div>
        <div class="panel-footer">
                <a type="button" class="btn btn-default btn-xm " href="#" id="retour" onclick="location.reload();" style="font-size: 11px;"><i class="fa fa-arrow-left"> </i> Retour</a>
            <button type="submit" class="btn btn-default btn-xm " style="background: rgb(70, 136, 71);color: white;font-size: 11px;" ><i class="fa fa-refresh"> </i> Appliquer</button>
                  <span class='tachat_img hide'><img src="{{ asset('/images/712.GIF') }}" alt="Second slide" class='img-circle' style="height: 20px;width: 20px;"> Mise à jour en cours ...</span>
        </div>
    </div>
</div>
           {{ form_rest(form) }}
</form>
<script>
     $(".chzn-select").chosen();
     $('form').draggable();
     $("form").submit(function(event) {
            var $this = $(this);
             $('.tachat_img').removeClass('hide');
             $.ajax({
                   type: $this.attr('method'),
                   url: $this.attr('action'),
                   cache: false,
                   data: $this.serialize(),
                   success: function() {
                     $('.tachat_img').addClass('hide');
                     location.reload();
                   },
                   error: function() {
                      // $('.tachat_img').removeClass('hide');
                   }
               });
               //we dont what the browser to submit the form
               event.preventDefault();
           });
</script></div>